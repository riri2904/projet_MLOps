name: PyTest

on : push

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: "3.x"

      - name: Execute tests
        run: pytest
